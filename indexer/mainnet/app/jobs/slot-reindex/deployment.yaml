apiVersion: apps/v1
kind: Deployment
metadata:
  name: slot-reindex
  annotations:
    reloader.stakater.com/auto: "true"
spec:
  replicas: 0
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: slot-reindex
  template:
    metadata:
      labels:
        app: slot-reindex
    spec:
      containers:
      - name: slot-reindex
        image: mpwsh/indexer-block-reindex:latest
        envFrom:
        - configMapRef:
            name: indexer-config
        - secretRef:
            name: meili-creds
        - secretRef:
            name: amqp-creds
        - secretRef:
            name: dialect-creds
        env:
        - name: DATABASE_WRITE_URL
          valueFrom:
            secretKeyRef:
              name: postgres-creds-url
              key: DATABASE_WRITE_URL
              optional: false
        - name: RUST_LOG
          value: info
        - name: START_SLOT
          value: "167121919"
        - name: END_SLOT
          value: "167848810"
        - name: THREADS
          value: "16"
        command: ["/bin/bash","-c"]
        args: ["./bin/slot_reindex reindex --network mainnet --selector-config /etc/reindex_config.json --sender reindex && echo Completed all blocks && sleep infinity"]
        securityContext:
          allowPrivilegeEscalation: false
        imagePullPolicy: Always
        volumeMounts:
        - name: reindex-config
          mountPath: /etc/reindex_config.json
          subPath: reindex_config.json
      volumes:
      - name: reindex-config
        configMap:
          name: reindex-config   
      dnsConfig:
        options:
        - name: ndots
          value: "3"
      securityContext:
        runAsUser: 1000
        runAsGroup: 3000
        fsGroup: 2000
